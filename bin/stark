#!/usr/bin/env ruby

require 'rubygems'
require 'stark/parser'
require 'stark/ruby'

data = File.read ARGV.shift

tg = Stark::Parser.new data

unless tg.parse
  tg.raise_error
end

ruby = Stark::Ruby.new

tg.result.each { |i| i.accept ruby }
